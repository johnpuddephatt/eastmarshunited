---js
{
  layout: "news",
  eleventyNavigation: {
    key: "News"
  },
  pagination: {
    data: "posts",
    size: 12,
    alias: "paginated_posts",
    before: function(data) {
      return process.env.ELEVENTY_ENV == 'development' ? data.slice(0,5) : data;
    }
  }
}
---